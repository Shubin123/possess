{
  "name": "poser",
  "private": true,
  "version": "0.1.0",
  "description": "In-browser pose classification: MediaPipe pose estimation plus a classifier you train yourself",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/",
    "test:e2e": "node e2e.test.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "puppeteer": "24.12.1"
  }
}
